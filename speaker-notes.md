Welcome everyone. I'm Daniel Wright from NavAIgate, an AI-native consultancy. Today we're going to combine two incredibly powerful tools — Claude Code and Google NotebookLM — to solve Claude's biggest limitation: its memory. By the end of this session you'll have a fully working setup with persistent memory, free content generation, and a growing AI brain that remembers everything across sessions.

Here's what we'll cover over the next hour. We'll start with the problem — why Claude forgets everything — then move into the solution. The middle chunk is all live demo: installing the skill, authenticating, and running through three real use cases. We'll wrap up with how to take this into Co-work and Q&A.

So here's the core problem. Every time you start a new Claude session, it's a blank slate. Claude Code does have some local memory, but it's limited. People try to solve this by reading loads of files at the start of each session — but that eats your context window and costs money in tokens. The more you read in, the faster you burn through your allowance.

The solution is Google NotebookLM acting as a second brain. Three core benefits: First, persistent memory — your decisions, rationale, and context survive across every Claude session. Second, cost reduction — instead of cramming hundreds of pages into Claude, you send one API call to NotebookLM and get back exactly what you need. It's free. Third, content multiplication — from a single source you can generate podcasts, videos, infographics, quizzes, all without burning Claude tokens.

Let's look at the six things NotebookLM brings to the table. Persistent memory — it remembers everything you put in it. Personal CRM — track contacts, meetings, follow-ups. Decision journal — record why you made choices so you can look back later. Content multiplication — turn one conversation into a podcast, video, or infographic. Deep research — it can ingest 40 to 70 sources and synthesise them into insights. And meeting intelligence — store transcripts and query them later.

This is the skill file we're about to install. Let me walk through what's in it. At the top, there's frontmatter — the name, description, and trigger phrases that tell Claude when to activate this skill. Then Step 0 handles all the setup: checking your Python version, creating a virtual environment, and installing the package. The authentication section is interesting — the built-in notebooklm login command doesn't work in Claude Code because it needs interactive keyboard input, so the skill uses a custom Playwright script instead. We'll see that in a moment. There's also a full command reference table, autonomy rules that define what Claude can run automatically vs what it asks permission for, and error handling.

Live demo time. I'm going to drop the skill file right into Claude Code and say "execute this skill." Watch what happens — Claude reads the file, checks our Python version, creates a virtual environment, installs the notebooklm-py package with browser support via pip, then runs "playwright install chromium" which downloads a headless Chromium browser. This is the same browser that'll be used for authentication in the next step. Finally it symlinks the CLI so we can call notebooklm from anywhere. Let's verify with notebooklm help.

Now why do we need a custom login script? The notebooklm package has a built-in login command, but it requires you to press Enter in the terminal after signing in. Claude Code's bash tool doesn't support interactive input — you can't press keys — so the browser just opens and immediately closes. The skill solves this by writing a custom Python script that uses Playwright — the same Chromium we just installed. It opens a browser window, you sign in to Google, navigate to notebooklm.google.com, then tell Claude you're done. Claude creates a small signal file, the script detects it, captures your session cookies, and saves them. No keyboard input needed. If auth fails, just delete the browser profile folder and try again.

Here's your cheat sheet. These are the commands you'll use most. You can list notebooks, create new ones, add sources from URLs, YouTube videos, or local files. You can chat with your notebook using the ask command, and run deep research that ingests dozens of web sources. Then you can generate content — podcasts, infographics, videos — and download them locally. Keep this slide handy — or better yet, refer to the skill file itself which has the complete table.

First use case: enrichment. The idea is simple — you're having a conversation with Claude about any topic, and you want to go deeper. Instead of Claude trying to research everything itself and burning tokens, you tell it to create a NotebookLM notebook with deep research. NotebookLM goes off, ingests 40 to 70 sources from the web, and builds a rich knowledge base. Then Claude makes one API call to query it and gets back distilled insights. The heavy lifting happens on Google's side for free.

Live demo. I'll type this exact prompt into Claude Code. Watch — it creates a new notebook, adds our conversation as a source, kicks off deep web research, waits for it to complete, then queries NotebookLM for the 10 distilled insights. One important tip: sometimes the deep research times out. If that happens, just ask Claude "did you import the deep research results?" and it'll finish importing them. It's a known quirk.

Second use case: content multiplication. This is where it gets really exciting. From a single conversation or notebook, you can generate eight different types of content — all for free on NotebookLM's side. Podcasts take 10 to 20 minutes to generate. Videos take longer, 15 to 45 minutes. But infographics and reports are quick, just a few minutes. Mind maps are instant. The key point here is: you're not paying for any of this generation. It all happens on Google's infrastructure.

Live demo. Let me generate an infographic right now. I'll type this prompt and Claude will send the request to NotebookLM. It generates the infographic on Google's side, and then we download it locally. Zero cost. And remember — you can do this with podcasts, videos, slide decks, quizzes, flashcards. Anything you can do in NotebookLM's web interface, you can now do programmatically from Claude.

Third and most powerful use case: the AI Brain. This is where we use the WrapUp skill. At the end of every session, you say slash wrapup and it does four things: reviews the entire conversation for key decisions and learnings, saves memories locally so Claude remembers in future sessions, writes a structured markdown summary, and pushes that summary into your AI Brain notebook in NotebookLM. Over time, this brain grows with every session you have. It becomes a semantic search engine over your entire Claude history.

Here's what's inside the WrapUp skill file. Step 0 automatically creates an "AI Brain" notebook in NotebookLM if you don't have one yet. Step 1 reviews the session for decisions, completed work, and open threads. Step 2 saves memories locally using Claude Code's memory system. Step 3 writes a markdown summary. And Step 4 pushes that summary to NotebookLM. You trigger it by saying slash wrapup, "wrap up", "save this session", or "end of session". Let me run it now so you can see it in action.

Here's the power move. Add this one line to your Claude project instructions: "Whenever answering questions about strategy, always consult the AI Brain notebook in NotebookLM first." Now every time you ask Claude a strategy question, it automatically does a semantic search across your entire session history in NotebookLM. One API call, instant recall. Think about it — you could have hundreds of sessions stored in there, and Claude can search through all of them semantically to find exactly what's relevant. And it costs nothing to query.

If you're using Claude Co-work instead of Claude Code, you can bring NotebookLM there too. The process is slightly different because Co-work runs in a sandbox — it can't read local files. So you ask Claude Code to generate a special version of the skill file that has your auth cookies baked directly into it. It strips down to only the essential cookies — about 1,400 tokens instead of 3,100. Then you upload that file as a skill in Co-work. When cookies expire, just come back to Claude Code and regenerate the file.

Let's recap the three pillars. Enrichment — deeper research and better decisions without burning tokens. Content multiplication — free generation of podcasts, videos, infographics from any conversation. And persistent memory — your AI Brain grows with every session and gives you semantic retrieval over your entire history. The key takeaway: your NotebookLM Brain is a semantic search engine over your entire Claude history, and it costs nothing to query.

Thank you all for joining. I'll share the two skill files with you — the NotebookLM skill and the WrapUp skill — so you can set this up yourself. If you want to see the original walkthrough, check out Jack Roberts' video "Claude Code plus NotebookLM equals Infinite Memory" on YouTube. Now let's open it up for questions.
